flowchart TB
    CP[Global Control Plane] -->|push rules + regional quotas| REG

    subgraph REG["Per-region rate limiting"]
        subgraph US["US Region — quota 500/min"]
            GW_US[Gateway] --> R_US[(Redis US)]
        end
        subgraph EU["EU Region — quota 300/min"]
            GW_EU[Gateway] --> R_EU[(Redis EU)]
        end
        subgraph APAC["APAC Region — quota 200/min"]
            GW_AP[Gateway] --> R_AP[(Redis APAC)]
        end
    end

    C_US([US clients]) --> GW_US
    C_EU([EU clients]) --> GW_EU
    C_AP([APAC clients]) --> GW_AP

    Note1["Global cap 1000/min<br/>slight overshoot OK if all regions max out"]
