flowchart TD
    ADM([Admin]) --> CP[Control Plane API]
    CP --> VAL{valid?}
    VAL -->|No| ERR([400])
    VAL -->|Yes| DB[(Rules DB)]
    DB --> BUS[Config Bus]
    BUS --> GW[Gateways — cached rules]
    GW --> REDIS[(Redis)]
